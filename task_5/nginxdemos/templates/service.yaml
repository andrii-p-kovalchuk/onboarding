apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginxdemos.fullname" . }}
  labels:
    {{- include "nginxdemos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginxdemos.selectorLabels" . | nindent 4 }}
